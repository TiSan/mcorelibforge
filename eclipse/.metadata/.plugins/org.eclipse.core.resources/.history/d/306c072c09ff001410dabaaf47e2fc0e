package de.tisan.forge.helpers;

import io.netty.buffer.ByteBuf;
import net.minecraft.nbt.NBTTagCompound;
import cpw.mods.fml.common.network.ByteBufUtils;
import cpw.mods.fml.common.network.simpleimpl.IMessage;
import cpw.mods.fml.common.network.simpleimpl.IMessageHandler;
import cpw.mods.fml.common.network.simpleimpl.MessageContext;
import cpw.mods.fml.relauncher.Side;

public class ServerToClientMessage implements IMessage, IMessageHandler<ServerToClientMessage, IMessage> {
	private NBTTagCompound tag;
	private Communication comm;

	protected ServerToClientMessage(NBTTagCompound tag, Communication comm) {
		this.tag = tag;
		this.comm = comm;
	}

	@Override
	public void fromBytes(ByteBuf buf) {
		tag = ByteBufUtils.readTag(buf);
	}

	@Override
	public void toBytes(ByteBuf buf) {
		ByteBufUtils.writeTag(buf, tag);
	}

	@Override
	public IMessage onMessage(ServerToClientMessage message, MessageContext ctx) {
		if(ctx.side.equals(Side.CLIENT)){
			comm.pushServerEvent(message.tag);
		}
		return null;
	}

}
